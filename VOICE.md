# VOICE.md

このファイルは、`articles/` の記事から抽出した筆者の文体と、書き方の規範をまとめたものである。記事を書くときは、まず直近10本を読み直して特徴を抽出し、その結果でこのファイルを更新したうえで執筆に入る。書きかけのまま止まっている下書きは、文体の見本にならないため抽出から除外する。

観点は以下の7つで、上ほど抽象的かつ重要、下ほど具体的な枝葉になる。抽出は思考特性に最も重きを置く。思考が揃えば、構成や表現は結果として似てくるためである。観点はそのまま本ファイルの章に対応する。

| 観点 | 章 | 着眼点 |
| :-- | :-- | :-- |
| 思考特性 | Mindset | 性格や興味関心の傾向 |
| 構成 | Structure | 章立て、タイトルや章見出しの書き方 |
| 段落 | Paragraphs | 文章の長さ、箇条書きや引用の使い方 |
| リズム | Rhythm | 抑揚の付け方、ユーモアや皮肉の交え方 |
| 語尾 | Endings | ですます調、問いかけ、体言止めの使い方 |
| 語彙 | Vocabulary | よく使う言い回しや表記の選び方 |
| 記法 | Notation | Markdownの書き方、段落や空白の取り方 |

## Mindset

筆者は、ツールそのものより「そのツールが人間の手間をどれだけ減らすか」に関心を向ける。認知負荷、必要最低限、本質といった言葉が記事をまたいで繰り返し現れ、判断の軸になっている。

新しい技術には乗るが、無条件に持ち上げない。流行の速さに疲れている自分を先に認め、そのうえで「だから簡単に使えるものを作った」「だからツールに任せることにした」と、面倒を減らす方向へ話を運ぶ。

権威や一般論をそのまま採らず、自分が試した結果を根拠にする。うまくいかなかった点、見落としていた点、まだ有料プランに手が届いていない点も、隠さずそのまま書く。

読者を置いていかない。専門用語には初出で短い言い換えを添え、前提知識がない人でも筋を追えるようにする。

## Structure

見出しは `##` を主とし、 `###` は列挙が続くときだけ使う。 `#` は記事タイトルが担うため本文では使わない。

章の型は、記事の性質によって次のいずれかを取る。

| 型 | 章立て | 使う場面 |
| :-- | :-- | :-- |
| 結論先出し | はじめに → 結論 → 解説 → おわりに | 手順や設定を渡す記事 |
| 導入統合 | はじめに（結論を含む） → 本論 → おわりに | 考え方を伝える記事 |
| 列挙 | はじめに → 対象読者 → 全体像 → 各論 → おわりに | ハマりどころをまとめる記事 |

見出しは文章にせず、1〜3語の簡潔な名詞句にする。目次として並べたときに記事の骨格が読み取れる粒度に揃える。番号や絵文字は付けない。

`## はじめに` で始め、 `## おわりに` で締める。この2つは省略しない。出典は脚注で示すため、 `## 参考` の節は設けない。

## Paragraphs

1段落は2〜4文にとどめ、段落間は必ず1行空ける。地の文だけが続く記事は読みにくいため、図・表・キャプチャ画像・コードブロックを積極的に挟み、読み手が視覚的に理解できる状態を保つ。挟まれた地の文は、空白を除いて `450` 字を上限とし、超える前に必ずいずれかを挟む。

| 手段 | 使う場面 |
| :-- | :-- |
| Mermaid | 処理の流れ、役割分担、構造の説明 |
| 表 | 複数の選択肢を同じ観点で比べるとき |
| キャプチャ画像 | 実際の画面や成果物を見せるとき |
| コードブロック | 設定値・コマンド・生成結果を示すとき |

コードブロックを置いたときは、そのコードが何をしているかの解説を地の文で添える。コードを貼っただけで次の話題へ移らない。

箇条書きは、並列の要素が3つ以上あるときに限って使う。列挙の前には必ず、なぜその要素群を出すのかを地の文で示す。要素が5つ以上になるか、複数の観点で比べたくなったら表に切り替える。

引用（ `>` ）は、プロンプトの文面や公式ドキュメントの原文をそのまま見せるときに使う。自分の言葉の強調には使わない。

## Rhythm

長い説明文のあとに短い一文を置いて、着地させる。「これだけです」「そういうものです」「この分担がすべてです」のような短文が、段落の切れ目で効く。

自問自答をはさんで話を折り返す。「では、わざわざ3つに分ける理由はどこにあるのか」「で、結局どういう仕様になったんだっけ？」のように、読者が抱く疑問を先に言語化してから答える。

ユーモアは、話の腰を折らない範囲で差し込む。取り消し線（ `~~面倒なので~~` ）、絵文字での感情表現（ `💢` ）、括弧書きの本音（「（ポエ）」「（本当）」）、自嘲（「うーん、なにもわからない」）が定番である。決め台詞のように多用せず、1記事に1〜2回にとどめる。

## Endings

ですます調で統一する。断定を避けず、推測は推測と分かる形で書く。体言止めは、対比を際立たせたいときだけ使う（「書くのは AI、見せるのはツール、読むのは人間」）。

語尾のパターンを以下に示す。

| 語尾 | 用途 | 例 |
| :-- | :-- | :-- |
| 〜んですよね | 実感を伴う指摘 | まったく読む気が起きないんですよね |
| 〜訳です | 論理の帰結 | 丸ごと Git の管理下に入る訳です |
| 〜ですね | 読者への同意の求め | いいところですね |
| 〜と思います | 自分の見立て | ようやく完成するのだと思います |
| 〜てみてはいかがですか？ | 締めの誘い | ぜひ使ってみてはいかがですか？ |

## Vocabulary

記事をまたいで繰り返し使う言い回しを以下に示す。新しい記事でも、無理のない範囲でこれらを織り込む。

| 言い回し | 位置 | 働き |
| :-- | :-- | :-- |
| 正直、 | 段落の頭 | 本音に切り替える合図 |
| という訳で | 転換 | 前段の理由から結論へ渡す |
| 要するに | まとめ | 長い説明を一言に畳む |
| 四の五の言う前に | 実演の直前 | 説明より先に動かす宣言 |
| 必要最低限かつ本質的 | 主張の核 | 判断基準そのもの |
| 認知負荷 | 主張の核 | 良し悪しを測る物差し |
| 正直、解説することはあまりないのですが | おわりにの入り | 気負いを外す |
| 地味に | 評価 | 小さいが効く、を示す |
| ぜひ | 締め | 読者を誘う |

日本語と英数字のあいだには半角スペースを入れる（例: `Pull Request の差分` ）。製品名・サービス名は正式表記に従い、初出でリンクを張る。

## Notation

太字は1章に1回程度にとどめ、主張の核だけに当てる。インラインコードは、コマンド・設定キー・ファイル名・具体的な値に使う。補足や注意は `:::message` に入れ、長い脱線は `:::details` に畳む。

図はすべて Mermaid で描き、画像ファイルや外部ツールに依存させない。コードブロックには言語とファイル名を添える（ ```` ```yml: .github/workflows/claude.yml ```` ）。設定値だけを見せるときは言語指定のみでよい。

出典は脚注で示す。本文中に番号を置き、記事の末尾にまとめる。ラベルは、ページ名とそのページを表示しているサービス名を、半角スペース・パイプ・半角スペースで区切って書く。

```markdown
[^1]: [yjn279/trinity | GitHub](https://github.com/yjn279/trinity)
[^2]: [Quick Start | Mintlify](https://mintlify.com/docs/quickstart)
[^3]: [mintlify/docs | GitHub](https://github.com/mintlify/docs)
```

引用した画像には、直後の行に出典を斜体で添え、そこからも脚注を参照する。

```markdown
![Mintlify が表示したページ](/images/022_agent-development-with-mintlify-03.png)
*Mintlify Documentation より引用 [^2]*
```

## Metrics

文体の定量面は `scripts/metrics.mjs` で測る。対象は見出し・コード・表・リスト・引用を除いた地の文である。特徴量抽出のたびに直近10本を測って下表を更新し、推敲では草稿を同じスクリプトで測って照らし合わせる。

| 指標 | 直近10本の値 |
| :-- | :-- |
| 記事の長さ | 平均 `1775` 字（ `588` 〜 `3299` 字） |
| 一文の長さ | 平均 `43` 字 |
| 段落の長さ | 平均 `78` 字 |
| 読点の頻度 | 一文あたり `1.0` 個 |
| 文末の分布 | ます `42`% ・ です `23`% ・ ました `8`% ・ 常体ほか `8`% ・ ません `5`% ・ ましょう `3`% |
| 絵文字の頻度 | 1記事あたり `1.7` 個 |
| ！？の頻度 | 1記事あたり `2.3` 個 |

数値は目標ではなく分布である。草稿がすべて平均に一致する必要はなく、分布から大きく外れた指標だけを直す。文末の `常体ほか` は、ですます調を外して着地させた文と体言止めを指す。Rhythm に書いた短い一文がここに現れる。

## Templates

記事の骨格として繰り返し使う定型を以下に示す。

社内で話した内容を記事にするときは、本文の先頭に断りを置く。

```markdown
:::message
この記事は、社内のLT会で発表した内容を雑にまとめたものです。
:::
```

導入は、困っていた状況から入り、何を使うことにしたかで閉じる。末尾には Front Matter の絵文字を再び置く。

```markdown
## はじめに

（何に困っていたか。読者が自分ごとにできる具体的な場面から書く）

（なぜ既存のやり方では足りないか）

という訳で、（採った手段）にしました🌿
```

締めは、話を一段引いた視座に戻してから読者を誘う。

```markdown
## おわりに

正直、解説することはあまりないのですが、（書いた動機）。

（この先どうなると考えているか）

ぜひ、（読者への誘い）！
```

## Hypotheses

会話の中でユーザーから受けた指示や、記事を書き終えた振り返りで得たルールは、各章へ直接書かずこの表に仮説として入れる。執筆時は各章のルールと同じ強さで適用する。

| ルール | 出所 |
| :-- | :-- |
| 「道具」ではなく「ツール」と表記する | 2026-08-09 の会話（本ファイルの整備時） |

汎用的なルールか、その場だけの事情かは、記事が実証する。特徴量抽出のたびに、この表を抽出対象の直近10本と照合する。 `2` 本以上の記事で確認できたルールは該当する章へ移し、確認できないまま出所より後の記事が `3` 本を超えたルールは、その場だけの事情だったと判断して削除する。
